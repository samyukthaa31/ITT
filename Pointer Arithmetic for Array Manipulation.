#include<iostream> 
using namespace std;
int main() 
{ 
    int arr[5],i; 
    int *p=arr; 
    cin>>*p>>*(p+1)>>*(p+2)>>*(p+3)>>*(p+4); 
    if(cin.fail())
    {
        cout<<"Invalid";
        return 0;
    }
    for(i=4;i>=0;i--) 
        cout<<*(p+i)<<" "; 
    return 0; 
}
